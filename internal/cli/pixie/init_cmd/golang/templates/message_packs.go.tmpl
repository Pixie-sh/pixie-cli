package message_packs

import (
	"context"
)

// Register registers message packs with the context
// Add your message pack registrations here
func Register(ctx context.Context) {
	// Register your message packs here
	// Example:
	// somePackage.SetContext(ctx, yourMessagePack)
}
