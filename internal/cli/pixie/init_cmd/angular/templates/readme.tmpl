# {{ .ProjectName }}

Angular frontend application generated by Pixie CLI.

## Prerequisites

- Node.js 20+
- npm 10+
- Angular CLI 19+

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```

2. Start development server:
   ```bash
   ng serve
   ```

3. Open http://localhost:4200

## Available Scripts

- `npm start` - Start development server
- `npm test` - Run unit tests
- `npm run lint` - Run linting
- `npm run build` - Build for production
- `npm run clean` - Clean build artifacts

## Environment Configurations

- **local** - Local development (`ng serve --configuration local`)
- **development** - Development server
- **staging** - Staging server
- **production** - Production (default build)

## Project Structure

```
src/
├── app/
│   ├── _core/           # Core services, models, and utilities
│   │   ├── api/         # API services
│   │   ├── services/    # Core services (auth, etc.)
│   │   ├── models/      # TypeScript interfaces
│   │   ├── enums/       # Enumerations
│   │   └── constants/   # Constants
{{- if .Features.state }}
│   │   └── stores/      # NGXS state management
{{- end }}
{{- if .Features.auth }}
│   ├── auth/            # Authentication screens
│   │   ├── sign-in/
│   │   ├── sign-up/
│   │   └── reset-password/
{{- end }}
│   ├── dashboard/       # Main dashboard
│   ├── layouts/         # Layout components
│   └── shared/          # Shared components and directives
├── assets/              # Static assets
{{- if .Features.i18n }}
│   └── i18n/            # Translation files
{{- end }}
├── environments/        # Environment configurations
└── styles/              # SCSS design system
```

## Features

{{- if .Features.auth }}
- **Authentication** - Sign in, sign up, password reset flows
{{- end }}
{{- if .Features.state }}
- **State Management** - NGXS with storage persistence
{{- end }}
{{- if .Features.pwa }}
- **PWA** - Progressive Web App with service worker
{{- end }}
{{- if .Features.i18n }}
- **i18n** - Internationalization support
{{- end }}
- **Design System** - SCSS variables with CSS custom properties
- **Bootstrap 5** - UI framework integration

## Learn More

- [Angular Documentation](https://angular.dev)
- [Angular CLI](https://angular.dev/tools/cli)
{{- if .Features.state }}
- [NGXS Documentation](https://www.ngxs.io)
{{- end }}
